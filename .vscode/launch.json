{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with Meson",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/build/pepito_app",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "build debug meson"
        }
    ]
}
